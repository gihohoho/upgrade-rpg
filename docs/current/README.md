# Current Documents — v372

이 폴더에는 **현재 판단이나 다음 승인에 직접 필요한 문서만** 둡니다.

## 계정·보안

- `CURRENT_STATUS.md`: 현재 checkpoint, 검증, 금지 경계
- `ACCOUNT_AUTH_AND_CHARACTER_SLOTS.md`: v370 인증·캐릭터 슬롯·저장 계약
- `ACCOUNT_EMAIL_VERIFICATION_RECOVERY_AND_DELETION.md`: v371 이메일 lifecycle과 migration 준비
- `SECURITY_ROTATION_AND_GITHUB_GATES.md`: secret, credential, 공개 전 보안 gate

## 배포

- `PRODUCTION_PROVIDER_SELECTION.md`: Render + Neon 비용 최소 선택
- `PRODUCTION_DEPLOYMENT_PLAN.md`: 운영 입력·승인·rollback
- `RENDER_SERVICE_SETTINGS_PLAN.md`: Render 서비스 설정
- `FRONTEND_STATIC_DEPLOYMENT_PLAN.md`: legacy Static Site 계획
- `BACKEND_IMAGE_GHCR_POLICY.md`: exact digest와 GHCR lifecycle
- `GITHUB_ACTIONS_GHCR_STATIC_WORKFLOW_PLAN.md`: 공급망 workflow 계약

## 구조

- `PROJECT_STRUCTURE.md`: 코드와 문서 폴더 지도

세부 기술 자료는 `../reference/`, 자동 보고서는 `../generated/`, 완료 이력은 `../archive/history/`에서 찾습니다.

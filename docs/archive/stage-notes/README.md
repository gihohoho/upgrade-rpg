# Stage Notes Archive

legacy 게임, 관리자 UI, backend 분리와 과거 기능 단계별 기록을 보관합니다. 현재 실행 계약은 아닙니다.

v334 구조 정리에서 `docs/` 루트에 흩어져 있던 고유 단계 문서를 이곳으로 이동했고, 이미 같은 내용이 있던 사본은 한 개만 남겼습니다. 현재 판단은 `../../current/`, 실제 가이드는 `../../guides/`, 계약은 `../../contracts/`를 사용합니다.

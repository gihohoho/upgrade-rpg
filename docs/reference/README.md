# Technical Reference

현재 단계가 바뀌어도 계속 참고하는 기술 자료를 주제별로 보관합니다.

- `database/`: PostgreSQL, Alembic, Neon, backup/restore
- `backend/`: FastAPI/backend 구조와 runtime 감사
- `frontend/`: Vue 전환, CORS, read-only API
- `assets/`: 장비 공식과 생성형 이미지 규칙·매핑

현재 승인과 다음 행동은 이 폴더가 아니라 `../current/`를 기준으로 판단합니다.
